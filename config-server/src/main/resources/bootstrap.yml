spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file://Users/sjh/Desktop/Twitter-Streaming-Analysis-Platform/config-server-repository
          clone-on-start: true
    fail-fast: true